# リスク登録簿 v0.1

> status: **draft**（AI 起草 2026-07-30。監査 Important-1 対応）
> 影響 = 高/中/低。各リスクは緩和策（既存 FR/NFR への参照）と撤退・停止条件を持つ。
> 見直し: スライス完了ごと＋リスク顕在化時。

| ID | リスク | 影響 | 緩和策 | 撤退・停止条件 |
|---|---|---|---|---|
| RSK-01 | ブラウザ自動化が媒体利用規約に抵触しアカウント BAN・凍結 | 高 | **規約が非 API 自動化を明示禁止する媒体（X）は事前 prohibited とし試行自体を行わない**（2026-08-01 PO 決定・BR-M-X-4）。その他はレート節度（NFR-7）・人間相当の操作間隔・媒体別日次上限（C）。1 アカウント 1 媒体で影響を局所化 | 同一媒体で 2 回目の警告/凍結 → 当該媒体の自動操作を停止し人手運用へ切替（tech-stack §7 の API 経路切替も検討） |
| RSK-02 | anti-bot 対策強化で攻略地図が恒常的に破損 | 中 | 三段構え（Playwright→Camoufox→ビジョン）＋自己修復 1 回→エスカレーション（FR-43） | 週 3 回以上の同一媒体破損 → tech-stack §7「API 経路へ切替」トリガー発動 |
| RSK-03 | 無人自走の暴走（不適切コンテンツの連続公開・スパム化） | 高 | 企画↔品質ペアゲート（FR-21）・倫理ゲート（FR-25）・束縛承認（FR-46）・レート上限。オートモード移行は実績証跡による機械判定のみ | ゲートをすり抜けた公開が 1 件でも検出 → オートモード即時解除・全公開を束縛承認へ戻す |
| RSK-04 | 法規違反（特定電子メール法・APPI・景表法） | 高 | PR 表記ゲート（FR-24）・オプトイン/配信停止導線の必須化（NFR-9, MR-HS-3, MR-LINE-2）・リード情報の SQLite 平文保持禁止 | 法規要件を機械ゲート化できない配信形態は採用しない（fail-close） |
| RSK-05 | credential・セッションの漏洩 | 高 | 暗号化ストア（FR-47）・平文検査（AC-47）・リポジトリ secret scan | 漏洩検知 → 全セッション失効・全パスワードローテーション後まで外部操作停止 |
| RSK-06 | SQLite 破損・データ喪失（証跡の消失） | 中 | 日次バックアップ＋世代保持（NFR-10）・WAL モード・遷移は書き込み後に実行（NFR-3） | バックアップから復元不能な破損 → 直近整合スナップショットへ巻き戻し、欠損期間の証跡を再取得 |
| RSK-07 | 有償支出の暴走（Seedance 等の例外 API） | 中 | spend_ledger 全件記録（FR-73）・月次上限で自動停止（NFR-6、暫定値あり） | 上限超過 → 有償経路タスク自動停止（実装済み動作、解除は人の config 変更のみ） |
| RSK-08 | 生成 AI Web UI の自動操作がサービス規約・検知に抵触（調査で 3 社とも明文禁止と確認 — BR-M-GENAI-1） | 高 | 保有アカウントのみ・レート節度・生成はインプット側限定（公開物は自前審査を通る）。採否自体を PO 判断へ（br-media §99-4） | アカウント警告 → 当該サービスの自動操作停止、素材調達を Canva/Seedance へ振替 |
| RSK-09 | Google アカウント連座停止（Gemini 自動操作等の規約違反が GA4・Search Console・Gmail まで波及） | 高 | Gemini を自動操作対象から除外（BR-M-GENAI-1）。基幹 Google アカウントと実験用アカウントの完全分離（BR-M-GENAI-2）。GA4/GSC は正規 API 経路へ（BR-M-MEAS-1） | 基幹アカウントへの警告 1 回で Google 系の全ブラウザ自動操作を恒久停止し API のみとする |
